// import { Query, Resolver, Arg, Mutation } from 'type-graphql';
// import Employee, { EmployeeInput } from '../schemas/employee';
// import { Employee as EmployeeModel } from '../../db/models/employee';
// import { v4 as uuidv4 } from 'uuid';

// @Resolver((of) => Employee)
// export default class {
//   @Mutation(() => Employee)
//   createCar(@Arg('model') model: EmployeeInput) {
//     return EmployeeModel.create({ id: uuidv4(), createdAt: new Date(), model });
//   }

// }
